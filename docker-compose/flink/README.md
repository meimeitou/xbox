# flink

[文档](https://nightlies.apache.org/flink/flink-docs-master/docs/deployment/resource-providers/standalone/docker/)

```shell
docker compose up -d
# 默认sql-client启动会自动退出，下面手动启动进入即可
# 使用sql client
docker compose run sql-client
```
