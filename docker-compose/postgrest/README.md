# PostgREST

This directory contains a `docker-compose` setup for running [PostgREST](https://postgrest.org/en/stable/), a standalone web server that turns your PostgreSQL database directly into a RESTful API.
